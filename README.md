# 课程表（PyQt + uv）

## 运行

```bash
uv sync
uv run python main.py
```

默认会读取当前目录下的 `classes.json`，并按周次显示课程。

## 功能

- 按 `classes.json` 自动生成周一到周日课程表
- 按 `2026-03-02` 为第 1 周周一，自动根据当天日期推断当前周
- 顶部使用左右箭头切换周次
- 在当前周内自动高亮“今天”所在列
- 自动显示本周生效课程（按 `weeks.start/end` 过滤）
- 同一时段课程冲突会高亮显示
